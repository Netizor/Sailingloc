{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php bin/console cache:warmup --env=prod && php bin/console doctrine:migrations:migrate --no-interaction --env=prod"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT public/index.php",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
